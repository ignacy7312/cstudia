#include "zeros.h"


int main(void){

    x = 1;
    y = 5;
    z = 6;
    zeros(x,y,z);

    x = 15;
    y = 1;
    z = 0;
    zeros(x,y,z);

    x = 0.5;
    y = 0.25;
    z = 7;
    zeros(x,y,z);

    return 0;
}